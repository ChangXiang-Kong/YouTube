<UserControl x:Class="BatchProcess3.Views.MainMenus.ActionsPageView"
             xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:actions="clr-namespace:BatchProcess3.Views.Actions"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mainMenus="clr-namespace:BatchProcess3.ViewModels.MainMenus"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="clr-namespace:BatchProcess3.ViewModels"
             d:DesignHeight="900"
             d:DesignWidth="1100"
             x:DataType="mainMenus:ActionsPageViewModel"
             Background="{DynamicResource PrimaryBackground}"
             Foreground="{DynamicResource PrimaryForeground}"
             mc:Ignorable="d">
  <Design.DataContext>
    <mainMenus:ActionsPageViewModel />
  </Design.DataContext>

  <Grid ColumnDefinitions="*" RowDefinitions="Auto, *">
    <!--  Header  -->
    <Grid Name="HeaderGrid">
      <StackPanel Height="100" ClipToBounds="True">
        <Image Source="{SvgImage /Assets/Images/background-actions.svg}" Stretch="UniformToFill" />
      </StackPanel>
      <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
        <Label HorizontalAlignment="Center" Classes="h1" Content="Actions" />
        <Label HorizontalAlignment="Center" Classes="sub2" Content="Create and manage all actions that can be used in a process" />
      </StackPanel>
    </Grid>

    <!--  Main TabControl Content  -->
    <Border Grid.Row="1" Padding="25">
      <TabControl x:Name="TabControl_Actions" Padding="0,5,0,0" SelectionChanged="ActionsTab_OnSelectionChanged">
        <TabItem Header="Print" IsSelected="True">
          <actions:PrintTabView />
        </TabItem>

        <TabItem Header="Custom Properties">
          <actions:CustomPropertiesTabView />
        </TabItem>

        <TabItem Header="File Info">
          <actions:FileInfoTabView />
        </TabItem>

        <TabItem Header="Save Model As">
          <actions:SaveModelTabView />
        </TabItem>

        <TabItem Header="Save Drawing As">
          <actions:SaveDrawingTabView />
        </TabItem>

        <TabItem Header="Import File">
          <actions:ImportFileTabView />
        </TabItem>

        <TabItem Header="Drawing Templates">
          <actions:DrawingTemplateTabView />
        </TabItem>

        <TabItem Header="Macros">
          <actions:MacrosTabView />
        </TabItem>
      </TabControl>
    </Border>
  </Grid>
</UserControl>